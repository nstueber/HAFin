[pytest]
# Ohne dies findet "import app.models" in tests/ das Paket nicht, wenn pytest nicht
# zufaellig aus haushaltsbuch/ heraus aufgerufen wird (siehe README, "Unit-Tests ausfuehren").
pythonpath = .
testpaths = tests
